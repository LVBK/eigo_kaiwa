
  <div class="section">
      <div class="container">
        <div class="row">
                                    <%= form_for(@goal) do |f| %>
  <% if @goal.errors.any? %>
    <div class="col-md-8 col-md-offset-2" id="error_explanation">
      <h3><%= pluralize(@goal.errors.count, "error") %> prohibited this goal from being saved:</h3>

      <ul>
      <% @goal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
          <div class="col-md-8 col-md-offset-2 white-form">
            <form role="form">

              <div class="form-group has-feedback">
                <label class="control-label" for="exampleInputEmail1">Email address</label>
                <input class="form-control" id="exampleInputEmail1"
                type="text" disabled="disabled" value="<%= current_user.email %>">
              </div>
              <div class="form-group has-feedback">
                <label class="control-label" for="exampleInputPassword1">Number of sentence</label><br>
                <%= f.number_field :number_of_sentence %>
              </div>
              <div class="form-group has-feedback">
                <label class="control-label">Deadline</label><br>
                <%= f.datetime_select :deadline %>
              </div>
              <div class="form-group has-feedback">
                <label class="control-label">Note</label><br>
                <%= f.text_area :note, :placeholder => "メモ。。。", cols: "40", rows: "7" %>
              </div>
              <div class="actions">
                <%= f.submit nil, :class => 'btn btn-primary' %>
              </div>
            </form>
<% end %>

          </div>
        </div>
      </div>
    </div>


